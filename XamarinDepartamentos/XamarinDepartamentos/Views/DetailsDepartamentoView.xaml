<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinDepartamentos.Views.DetailsDepartamentoView">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding IdDepartamento
                , StringFormat='Id departamento: {0}'}"
                VerticalOptions="Start" 
                   FontSize="25"
                HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding Nombre, StringFormat='Nombre: {0}'}"
                   FontSize="25" TextColor="Blue"
                   HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding Localidad, StringFormat='Localidad: {0}'}"
                   FontSize="25" TextColor="Red"
                   HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>